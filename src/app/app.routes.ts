import { Routes } from '@angular/router';
import { Meals } from './components/meals/meals';

export const routes: Routes = [
    { path: 'meals', component: Meals },
// { path: 'meal/:id', component: MealDetails } // coming soon

];
